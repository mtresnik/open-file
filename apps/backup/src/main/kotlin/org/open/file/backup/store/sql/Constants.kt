package org.open.file.backup.store.sql

/** Matches the naming used by :apps:snapshot:sql so every module's DB lives alongside snapshots.db / nodes.db under `~/.open-file/`. */
const val BACKUPS_DB_FILE_NAME = "backups.db"
