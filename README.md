# open-file

## sub projects

### template

- Create a template for a commonly used directory or file, along with any needed configs.
- TODO: Export templates to Dockerrun files

### snapshot

- TODO: Create a `snapshot` of a directory or a drive. This allows directory information to be stored
without the need for the directory or drive to exist. Should probably add a generic `sync` command.

### TODO

- Be able to swap the database implementation from sql to .git if possible.
